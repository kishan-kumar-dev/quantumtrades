export enum Role {
  Trader = "trader",
  Admin = "admin",
}

export enum OrderSide {
  Buy = "buy",
  Sell = "sell",
}

export enum OrderType {
  Market = "market",
  Limit = "limit",
}

export enum OrderStatus {
  Open = "open",
  Filled = "filled",
  Cancelled = "cancelled",
}
